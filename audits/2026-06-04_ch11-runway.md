# Ch-11 runway — authoring gate cleared (2026-06-04)

Follow-on to [`2026-06-04_ecosystem_checkpoint.md`](2026-06-04_ecosystem_checkpoint.md). Clears the four
Ch-11 authoring-gate findings (checkpoint §2) and builds the two deferred scaffolding artifacts
(checkpoint §6), so Ch 11 authoring starts clean. Work on branch `chore/ch11-runway` (PR #15 — the
checkpoint — was merged to `main` first). Status markers bracketed per the repo audit convention.

## Gate findings resolved

| ID | Finding (as carried forward) | Resolution this session | Status |
|---|---|---|---|
| 0527-F6 | Mamba-3 absent from `bibliography.bib` (~13 uncited mentions) | **Stale.** `lahoti2026mamba3` is present (`bibliography.bib:215`), added during Ch 10 authoring; the checkpoint carried F6 forward without re-verifying. No action. | `[RESOLVED — stale]` |
| 0527-F4 | XRef machinery declared but dead (0 `<XRef>` consumers) | **Adopted forward-only.** The IDs were never dead — 58 feed `labels.json` (anchors/numbering) and `ChapterHeader.astro` consumes it. The unused part was the in-prose `<XRef>` component, which is production-quality and **already CI-enforced** (`book-scaffold validate` fails on unknown ids). STYLE.md §4 rewritten: real `<XRef>` example + policy (backward object-refs use `<XRef>` from Ch 11 on; prose for §/chapter/forward refs; opportunistic Ch 1–10 retrofit). The false "examples in Ch 1–6" pointer removed. | `[RESOLVED — decided]` |
| 0527-F27 | torch parity — ch04–06 torch companions absent | **Backfilled.** 6 torch ports (ch04–06, compute-and-parity only, no figures) + parity tests — **53 new tests, max residual ~1.3e-14**, all direct JAX↔torch parity at `< 1e-9`. torch is now complete for **ch01–10**. Makefile `companion-torch-tests` extended; each chapter's §X.10 gained a PyTorch subsection. Policy codified in STYLE.md §8 (JAX canonical; torch parity going forward; Julia selective). | `[RESOLVED]` |
| 0527-F26 | no pytest assertion suite for JAX companions | **Re-derived.** ch07–10 JAX already had suites (finding stale). The proposed "backfill `__init__.py`" mechanism was tested and **broke collection** — `test_discretization.py` exists in *both* ch04 and ch10, and partial `__init__.py` shims create colliding `tests` packages under pytest's prepend mode. Fixed properly: `--import-mode=importlib` in `pytest.ini` + removed **all** `tests/__init__.py` shims. Bar codified in STYLE.md §8 (exact `< 1e-12`, parity `< 1e-9` float64, figure-pinning). JAX 144 + torch 46 baselines green; 53 new torch tests green. | `[RESOLVED — re-derived]` |

## Environment note
The committed-path `.venv` is gitignored and was **headless** this session (directory present, no
interpreter — a fresh clone has none). Recreated with `uv venv` + `uv pip install -e
'companions/_shared[torch]'` + `pytest`. Companion verification requires this local venv; CI stays
jax/torch-free by design (`make check` ≠ `make check-local-torch`).

## Handoff correction (verified)
Merging **PR #15 did NOT** trigger a production deploy — its paths (`.claude/`, `audits/`, `docs/`,
`CLAUDE.md`, `README.md`) don't match `deploy.yml`'s `src/**` filter. The **Ch-11-runway PR *will*
deploy** (the ch04–06 §X.10 prose edits touch `src/content/chapters/`); the merge is left to the author.

## Scaffolding built (checkpoint §6)
- `docs/DASHBOARD.md` — release-trust scoreboard (17 chapters; includes a refresh snippet to resist drift).
- `docs/templates/chapter-research-brief.md` — per-chapter prep template (generalizes the Ch 11 pre-recon).

## Still open (not this session)
- GH `#1` (standards-hardening umbrella), `#4` (ch04 Julia in default gate).
- `0604-F1` (README "the user's" → "the author's", trivial); `0604-F2` (`package.json` v4.8.0 → v4.14.2).
- Ch 1–10 have **not** been through `claim-skeptic` (new 2026-06-04; a forward gate for Ch 11+).
